# Project - URL Checker

## Project Start

For this project, we will be creating a concurrent URL Checker. To run it, you can simply do:

```bash
scripts/run_url_checker.sh
```

You will then be able to see the logs in the terminal accordingly.
